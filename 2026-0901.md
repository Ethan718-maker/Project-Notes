純文字版研究筆記】

[專案研究筆記：Supergrid Graph 支配集與 GNN 訓練實務]

我們在解決什麼問題？（Dominating Set 支配集）

核心概念：假設地圖是 5x5 的棋盤，每個結點是房間。我們要在最少房間擺放「守衛（藍點）」，讓守衛看管自己與相鄰的所有房間。

最終目標：用最少數量的守衛涵蓋 100% 的地圖。

三大方法成果比對 (5x5 地圖測試)

ILP（數學精確解）：耗時 0.424 秒，算出最少需要 4 個守衛（標準答案）。

Greedy（傳統貪婪演算法）：使用了 4 個守衛。

GCN（我們訓練的 AI 模型）：同樣只用了 4 個守衛，證明 AI 在幾毫秒內就能達到 100% 的理論最佳解！

今日環境與實作重大突破

環境建置：成功使用 uv 建立 .venv 虛擬環境，解決 Python 3.14 (PEP 668) 的套件隔離問題。

套件掛載：完整安裝 torch、torch-geometric (PyG)、matplotlib 與 pulp (ILP 解算器)。

模型訓練：成功透過選單生成 100 張網格資料集，完成 GCN 的兩階段訓練（50 Epochs 監督式學習 + 200 Episodes 強化學習微調）並儲存權重檔 (.pt)。