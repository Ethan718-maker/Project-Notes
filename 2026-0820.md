📝 專題進度與學習筆記 (README.md)
📚 第一部分：專題核心專有名詞白話辭典 (Glossary)
為了避免日後遺忘，將專題與計畫書中的關鍵專有名詞整理如下：

1. 超網格圖 (Supergrid Graph)
白話解釋：就是「有挖洞、不規則的格子地圖」。

為什麼叫超網格？：一般的網格（Grid）是像棋盤一樣方方正正的 N×M 格子。但在現實應用（如晶片佈線、機器人地圖）中，中間會有障礙物或被挖空的區域。「超網格」就是把這些不存在的格子（洞）拔掉後剩下的地圖。

程式裡的特徵：因為形狀不規則，沒辦法用傳統的二維矩陣（陣列）存取，必須把它當成 Graph（圖），只記錄「還存在的格子」以及「誰跟誰相鄰」。

2. 最小支配集 (Minimum Dominating Set, MDS)
白話解釋：「用最少數量的守衛（保全），看守住全圖每一格」。

遊戲規則：

放在某格的守衛，可以看管「自己這一格」以及「上下左右相鄰的鄰居格子」。

最終目標是讓整張地圖 100% 被覆蓋，不能有任何一格是死角。

「最小」代表守衛數量要越少越好（節省資源/成本）。

3. GNN (Graph Neural Network, 圖神經網路)
白話解釋：一種專門用來讀「點與線關係」的 AI 模型。

為什麼一般 AI 不行？：一般 AI 只能讀固定大小的圖片或文字；但超網格地圖因為會挖洞，每次地圖的點數量不同、形狀也不同，傳統 AI 會卡住，只有 GNN 處理得了。

4. 訊息傳遞 (Message Passing)
白話解釋：「每個格子向鄰居打聽情報」。

運作機制：

第 0 輪：每格只知道自己的基本資料（例如：自己的座標、周圍有幾個鄰居）。

第 1 輪：每格跑 for 迴圈向隔壁鄰居問訊，把鄰居的特徵加總/平均過來。

第 2 輪：再向鄰居問一次，就能間接知道「兩步以外」的地形變化。

結果：問了幾輪後，GNN 會替每一格打分數（0.0∼1.0 的機率），代表這格有多適合放守衛。

5. ILP (Integer Linear Programming, 整數線性規劃)
白話解釋：「用數學公式暴力算出的 100 分標準答案」。

優缺點：答案絕對是最優解（守衛最少），但計算速度極慢。地圖一放大（例如 50×50 以上），電腦就會算到當機。

6. Greedy (貪婪演算法)
白話解釋：「憑當下直覺選最多鄰居的那格」。

優缺點：計算速度極快（毫秒級），但選出來的守衛數量不一定是最少（可能是 85 分的答案）。

🎯 第二部分：專題整體流程與策略 (System Architecture)
若偉教授這份 7000+ 行程式碼（Dom-SupergridGrid_v38.py）的核心運作邏輯如下：

題目與標準答案準備：程式產生大量的超網格地圖，並用 ILP 算出標準答案（哪些點放守衛）。

AI 學習：把地圖轉成 Graph，教 GNN 學習標準答案。

GNN 快速猜測：訓練好後，遇到新地圖時讓 GNN 盲猜，輸出每點的機率值（對應 gnn_raw_only）。

Greedy / ILP 補洞：如果 GNN 盲猜完後發現還有死角沒蓋滿，最後再用 Greedy 快速把剩下的死角補齊（對應 gnn_raw_then_complete）。

💻 第三部分：團隊硬體算力盤點與因應策略
為了評估大尺寸超網格圖執行的可行性，進行組內規格盤點：

成員	CPU 規格	DRAM	VRAM	專題分工與策略
Ethan (我)	R7 4800H (8核16緒)	16 GB	8 GB	核心測試機：負責小尺寸網格（10×10 內）的 GNN 邏輯驗證與模型跑通。
組員	i5-10300H (4核8緒)	16 GB	6 GB	輔助開發機：負責 Cursor 程式碼模組化拆解、Git 筆記維護與資料整理。
記憶體瓶頸與解決方案：兩台電腦 DRAM 均為 16GB，無法直接執行大尺寸（如 50×50 以上）的完整 GNN 訓練。目前策略為先將實驗地圖尺寸限制在 10×10 內，避免記憶體溢位。

⏰ 第四部分：個人時間規劃與約束條件 (Constraints & Schedule)
為兼顧打工體力、課業檢定與專題進度，擬定以下時間規劃：

1. 打工體力限制與開發時段
打工時間：每週 4 天（18:00 ～ 23:30）。

開發時段：打工當日下班後體力已透支，不安排深度寫程式。專題開發與論文閱讀嚴格集中於「無打工的 3 天」以及日間課餘空檔進行。

2. CPE 大學程式能力檢定準備
考試時程：10 / 14 參加 CPE 檢定（若未通過，預計 11 月進行補考）。

雙軌學習策略：10/14 前的專題練習（如：用 Python 手寫基础 4×4 Greedy 支配集演算法、練習 List 與迴圈），將直接作為 CPE 程式檢定的基礎邏輯訓練。

📌 第五部分：目前學習進度與下一階段目標 (Next Steps)
當前進度 (Current Progress)
[x] 成功將 C 語言基礎（迴圈、scanf/printf）對接至 Python 語法（縮排、input() 轉型、list、for i in range）。

[x] 完成 Cursor 開發環境設定與中文化。

[x] 完成團隊硬體算力盤點與 Git 筆記庫建立。

[x] 完全理解 Graph、GNN、超網格圖與支配集之白話邏輯。

下階段預計執行事項 (Next Steps)
建立固定時間表：排定每週無打工時段的專題固定開發時間。

小模組手寫練習：在 Cursor 環境下，嘗試用 Python 手寫 4×4 小地圖的「Graph 鄰居表 adj」與「Greedy 守衛選點」邏輯。

CPE 每日練習：白天空檔抽空完成 1~2 題 C/Python 基礎邏輯題，準備 10/14 CPE 檢定。